info:
  name: Ungültiges Rating XXX
  type: http
  seq: 19

http:
  method: GET
  url: "{{restUrl}}?rating=XXX"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: rating
      value: XXX
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
