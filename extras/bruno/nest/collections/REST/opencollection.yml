opencollection: 1.0.0

info:
  name: REST
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  variables:
    - name: restUrl
      value: "{{baseUrl}}/rest"
bundled: false
extensions:
  ignore:
    - node_modules
    - .git
